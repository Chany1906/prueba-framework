pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Repositorio conectado correctamente'
            }
        }

        stage('Test') {
            steps {
                echo 'Pipeline ejecutado desde Jenkinsfile'
            }
        }
    }
}